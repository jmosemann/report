[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"
  included_files = []

[dev]
  command = "vite --host 0.0.0.0"
  port = 8888
  targetPort = 5173
  framework = "#custom"
